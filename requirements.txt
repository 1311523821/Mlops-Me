# ============================================================
# MLOps 工作流 — Python 依赖
# ============================================================
# 安装方式: pip install -r requirements.txt
#
# 如果你只用 CPU 推理，可以把 torch 和 torchvision 的 CUDA 版本
# 换成 CPU 版: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ============================================================

# ---------- 深度学习框架 ----------
torch>=2.0.0
torchvision>=0.15.0

# ---------- MLOps 追踪 ----------
mlflow>=2.0.0

# ---------- 配置和数据处理 ----------
ruamel.yaml>=0.17.0
numpy>=1.24.0
pillow>=10.0.0
scikit-learn>=1.3.0

# ---------- ONNX 导出和推理 ----------
onnx>=1.14.0
onnxruntime>=1.15.0

# ---------- 可视化和评估 ----------
matplotlib>=3.7.0

# ---------- 推理服务 ----------
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
